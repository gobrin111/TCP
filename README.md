# TCP
http requests and websocket handling from scratch


To host and run the App, download the repo.


Then "docker rm -vf $(docker ps -aq)" in terminal to make sure that the docker images are clean.


Then "docker compose up --build --force-recreate" in terminal.


Navigate to a browser and go to localhost:8080
